# APFNet

This repository provides the training and testing code for APFNet, together with the experimental results.

## Repository Structure

- `code/`: Contains the training and testing code.
- `results/`: Contains Excel files reporting the results of all evaluated methods on five datasets.

## Usage

Please configure the dataset paths and experimental settings in the corresponding scripts before running the training or testing code.

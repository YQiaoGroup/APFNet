1. **`train/`**: Contains the training source code and the required dataset, taking the abdominal CT dataset as an exemplar.
2. **`test_example/`**: Provides the evaluation/testing script along with two sample test cases from the abdominal CT dataset.
3. **`results/`**: Includes the open-source benchmarks (Excel sheets) comparing our proposed method against various baselines, as well as the pre-trained model checkpoints (`.pth` files) across different datasets.
